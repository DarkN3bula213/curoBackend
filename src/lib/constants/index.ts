export const enum Header {
    API_KEY = "x-api-key",
    AUTHORIZATION = "authorization",
  }
  export enum ValidationSource {
    BODY = "body",
    HEADER = "headers",
    QUERY = "query",
    PARAM = "params",
  }
  